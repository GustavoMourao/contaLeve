---
import Section from '../primitives/Section.astro';
import Container from '../primitives/Container.astro';
import Heading from '../primitives/Heading.astro';

interface Props {
  heading?: string;
  items: Array<{ title: string; body: string }>;
}
const { heading, items } = Astro.props;
---
<Section surface="muted">
  <Container>
    {heading && (
      <Heading level={2} class="text-center mb-12">{heading}</Heading>
    )}
    <ul class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
      {items.map((item) => (
        <li class="rounded-card border-[2.5px] border-ds-charcoal bg-ds-white p-6 shadow-ds-sm">
          <h3 class="font-display text-lg font-semibold text-ds-charcoal">{item.title}</h3>
          <p class="mt-2 text-ds-gray leading-relaxed">{item.body}</p>
        </li>
      ))}
    </ul>
  </Container>
</Section>
